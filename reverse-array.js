// creating a new array numbers to be reversed / Declaring an array.
let numbers = [20,50,30,60,200];

// Use the reverse() method to reverse the array in place.
numbers.reverse()


// Output the original and reversed arrays
console.log(numbers);
